{
  "name": "pi-zh",
  "version": "0.7.0",
  "description": "Pi 中文用户的实用工具集 — 命令/快捷键中文说明、费用雷达、Codex 出口守卫",
  "keywords": [
    "pi-package",
    "chinese",
    "zh",
    "localization",
    "pi-extension",
    "zh-commands",
    "codex-safe",
    "proxy"
  ],
  "scripts": {
    "check": "npx tsx scripts/check-coverage.ts",
    "test": "vitest run",
    "preversion": "npm run check"
  },
  "license": "MIT",
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "devDependencies": {
    "vitest": "^5.0.0"
  }
}
